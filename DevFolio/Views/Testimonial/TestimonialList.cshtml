@model List<DevFolio.Models.TblTestimonial>
@{
    ViewBag.Title = "TestimonialList";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<h2>Referans Listesi</h2>

<div class="container">
    <a href="/Testimonial/CreateTestimonial/" class="btn btn-info">Yeni Referans</a>
    <br />
    <br />
    <table class="table table-bordered">
        <tr class="table-secondary">
            <th>#</th>
            <th>Ad Soyad</th>
            <th>Açıklama</th>
            <th>Resim</th>
            <th>Durum</th>
            <th>Sil</th>
            <th>Güncelle</th>
        </tr>

        @foreach (var item in Model)
        {
            string durum = "";
            if (item.Status == false)
            {
                durum = "bg-danger";
            }
            else
            {
                durum = "bg-secondary";
            }

            <tr>
                <td>@item.TestimonialID</td>
                <td>@item.NameSurname</td>
                <td>@item.Description</td>
                <td>@item.ImageUrl</td>
                <td><a href="/Testimonial/StatusChangeTestimonial/@item.TestimonialID"> <span class="badge @durum" style="color:white">@item.Status</span></a></td>
                <td><a href="/Testimonial/DeleteTestimonial/@item.TestimonialID" class="btn btn-warning">Sil</a></td>
                <td><a href="/Testimonial/UpdateTestimonial/@item.TestimonialID" class="btn btn-success">Güncelle</a></td>
            </tr>
        }


    </table>
</div>

